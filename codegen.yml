overwrite: true
schema: ${BACKEND_URL}
documents: "graphql/**/*.graphql"
generates:
  graphql/generated.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
